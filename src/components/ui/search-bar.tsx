/* import { Input, InputProps } from "./input";

interface SearchBarProps<T>  extends Omit<InputProps, 'type'> { 
  list: T[];
}

export function SearchBar<T>({list, ...props}: SearchBarProps<T>) {

  return (
    <div className="flex flex-col gap-2 bg-slate-300 p-4">
      <Input placeholder="Pesquisar..."  />

    </div>
  );
} */